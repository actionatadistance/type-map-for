{
  "name": "type-map-for-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test":  "vitest run --typecheck"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest":     "^4.1.7"
  }
}
